import { Box, Typography } from '@mui/material';
import React from 'react';

const Pay = () => {
    return (
        <Box>
            <Typography>Payment is coming soon</Typography>
        </Box>
    );
};

export default Pay;